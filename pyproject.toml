[project]
name = "compress-pdf"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pymupdf>=1.28.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.3",
]
